<%= form_for @picture, :html=> {:multipart => true} do |f| %>
	<p> 
		<%= f.label :artist %> <br>
		<%= f.text_field :artist %>
	</p>
	<p>
		<%= f.label :title %> <br>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.file_field :image %>
	<p>
		<%= f.submit "Save" %>
	</p>

<% end %>
