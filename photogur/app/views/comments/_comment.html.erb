<p>
	<strong> Commenter:</strong>
	<% current_user.id != nil %> 
	<!-- how can I get the user's id/email to sho up as the commenter?  -->
		<%= comment.commenter %> 
	<% end %>
</p>

<p>
	<strong>Comment:</strong>
	<%= comment.body %>
</p>

<p> 
	<%= link_to "Delete Comment", [comment.picture, comment],
		method: :delete,
		data: {confirm: "Are you sure?"} %>
</p>